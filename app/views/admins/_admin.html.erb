<tr>
  <td><%= admin.name %></td>
  <td>
      <%= link_to 'Edit', edit_admin_path(admin) %> |
      <%= link_to 'Delete', admin, method: :delete, data: { confirm: "Are you sure you want to delete #{ admin.name }?" } unless admin == current_user %>
  </td>
</tr>