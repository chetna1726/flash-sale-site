<tr>
  <td><%= user.name %></td>
  <td>
      <%= link_to 'View', user %> |
      <%= link_to 'Edit', edit_user_path(user) %> |
      <%= link_to 'Delete', user, method: :delete, data: { confirm: "Are you sure you want to delete #{ user.name }?" } unless user == current_user %>
  </td>
</tr>