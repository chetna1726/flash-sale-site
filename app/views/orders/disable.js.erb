$('#status').html('Disabled')
$('[data-id="<%= @admin.id %>"]').html('<%= j link_to "Enable", enable_admin_path(@admin), data: { confirm: "Are you sure you want to enable #{ @admin.first_name }?" }, class: "btn btn-xs btn-success", remote: true %>');